config {
    type: "view",
    schema: "staging", 
    tags: ["staging", "geo"], 
    assertions: {
        nonNull: ["location_id", "city_id"], 
        uniqueKey: ["location_id"]
    }
}

select *, locations -- intentional mistake, no such field in my source
from ${ref("locations")}
