config {
    type: "view",
    schema: "staging", 
    tags: ["staging", "geo"], 
    assertions: {
        nonNull: ["location_id", "city_id"], 
        uniqueKey: ["location_id"]
    }
}

select 
    location_id
    , city_id
    , full_address
from ${ref("locations")}
