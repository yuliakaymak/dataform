config {
    type: "view",
    schema: "staging", 
    tags: ["staging", "sales"], 
    assertions: {
        nonNull: ["id", "sales_date", "sku_id", "location_id", "sales_repr_id"], 
        uniqueKey: ["id"]
    }
}

select *, unknown_column
from ${ref("sales")}
