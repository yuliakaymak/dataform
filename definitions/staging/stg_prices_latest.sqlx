config {
    type: "view",
    schema: "staging", 
    tags: ["staging", "sales"], 
    assertions: {
        uniqueKey: ["sku_id"]
    }
}

select *
from ${ref("stg_prices")}
where _is_active = 1
